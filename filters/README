A filter, if actived, applies to the body of an entry before being passed to
a converter. Each filter must be a submodule that defines a dict called
`filter_register` with at least one required argument `callback`. This method
takes a single argument (the body text) and returns a modified version of it.

The following filters are known:

 * markdown - converts body text using a port of John Gruber's markdown script
